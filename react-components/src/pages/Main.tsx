import React, { Component } from 'react';

class Main extends Component {
  constructor(props: Record<string, never>) {
    super(props);
  }

  render() {
    return <div>Main</div>;
  }
}

export default Main;
